{
  "name": "kle-ad-dashboard",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "start:deploy": "npm --prefix backend run seed && npm --prefix backend run import && npm --prefix backend start"
  }
}
